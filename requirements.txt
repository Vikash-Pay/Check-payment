# === CORE DEPENDENCIES ===
aiogram==3.6.0
python-dotenv==1.0.1
aiosqlite==0.19.0
aiofiles==23.2.1
psutil==5.9.7

# === WEB SERVER (Render requires web process) ===
aiohttp==3.9.1
uvicorn[standard]==0.27.0
fastapi==0.104.1  # Optional: for health checks

# === UTILITIES ===
async-timeout==4.0.3
cryptography==42.0.5
python-dateutil==2.8.2
pytz==2024.1
filetype==1.2.0
structlog==23.2.0

# === OPTIONAL OPTIMIZATIONS ===
uvloop==0.19.0  # Faster async loop (Linux only)
ujson==5.8.0    # Faster JSON parsing
orjson==3.9.10  # Alternative fast JSON
